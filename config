# ===================
# GHOSTTY MAIN CONFIG
# ===================
# This file imports all configuration modules
# Edit individual files for specific settings

# Theme and color settings
config-file = theme.conf

# Appearance and window settings
config-file = appearance.conf

# Quick terminal configuration
config-file = quick-terminal.conf

# Keybindings
config-file = keybinds.conf

# Utilities and experimental settings
config-file = utils.conf
